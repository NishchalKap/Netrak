{
  "name": "netrak",
  "version": "1.0.0",
  "private": true,
  "description": "Netrak public safety platform",
  "scripts": {
    "install:gateway": "npm install --prefix backend/gateway",
    "install:operations": "npm install --prefix apps/operations",
    "prisma:generate": "cd backend/gateway && npx prisma generate",
    "lint": "npm run lint --prefix backend/gateway && npm run lint --prefix apps/operations && npm run lint --prefix apps/mobile",
    "typecheck": "npm run typecheck --prefix backend/gateway && npm run typecheck --prefix apps/operations && npm run typecheck --prefix apps/mobile",
    "build": "npm run build --prefix backend/gateway && npm run build --prefix apps/operations",
    "dev:operations": "npm run dev --prefix apps/operations",
    "dev": "npm run dev --prefix backend/gateway",
    "start": "npm run start --prefix backend/gateway"
  }
}
